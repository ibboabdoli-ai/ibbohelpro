{
  "name": "cleanai-mvp",
  "private": true,
  "scripts": {
    "dev": "vite",
    "dev:api": "node api/dev-server.mjs",
    "dev:full": "node scripts/dev-full.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm run build && npm audit --audit-level=moderate",
    "test:stage2": "node scripts/test-stage2-ai.mjs",
    "check:stage2": "npm run build && npm run test:stage2 && npm audit --audit-level=moderate",
    "test:stage3": "node scripts/test-stage3.mjs",
    "check:stage3": "npm run build && npm run test:stage2 && npm run test:stage3 && npm audit --audit-level=moderate"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "autoprefixer": "^10.4.23",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.18",
    "vite": "^7.3.0"
  }
}
